{
  "name": "task-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "start.bat",
    "dev:local": "start-local.bat",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "db:migrate": "npx --prefix backend prisma migrate dev",
    "db:studio": "npx --prefix backend prisma studio"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
